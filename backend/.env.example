# Environment: production, development, staging
ENV=development

# Security: Replace with long random strings in production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET="DEVELOPMENT_SECRET_REPLACE_IN_PROD_123"

# Database
# Development: sqlite:///./clinical_doc.db
# Production: postgresql://user:password@host:port/dbname
DATABASE_URL=sqlite:///./clinical_doc.db

# Frontend URL (required)
FRONTEND_URL=http://localhost:3000

# AI API Keys (at least one required for AI features)
GROQ_API_KEY=GROQ_API_KEY


# CORS: Comma-separated list of allowed origins (optional, FRONTEND_URL is auto-included)
BACKEND_CORS_ORIGINS=""
